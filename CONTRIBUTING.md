# 🤝 دليل المساهمة في CanCare

شكراً لاهتمامك بالمساهمة في تطبيق CanCare! نحن نرحب بجميع أنواع المساهمات.

---

## 📋 جدول المحتويات

1. [مبادئ المساهمة](#مبادئ-المساهمة)
2. [كيفية المساهمة](#كيفية-المساهمة)
3. [معايير الكود](#معايير-الكود)
4. [عملية المراجعة](#عملية-المراجعة)
5. [الإبلاغ عن الأخطاء](#الإبلاغ-عن-الأخطاء)
6. [اقتراح ميزات جديدة](#اقتراح-ميزات-جديدة)

---

## 🎯 مبادئ المساهمة

### التصميم أولاً
عند المساهمة في CanCare، يجب الحفاظ على المبادئ التصميمية:

#### 1. **راحة المريض أولاً** 💚
- استخدم ألوان هادئة ومطمئنة
- خطوط كبيرة وواضحة (19-36 بكسل للمرضى)
- أزرار كبيرة سهلة الضغط (64 بكسل)
- مساحات بيضاء سخية

#### 2. **سهولة الاستخدام** 👆
- واجهات بسيطة ومباشرة
- تنقل واضح وبديهي
- رسائل واضحة ومطمئنة
- أيقونات بديهية

#### 3. **إمكانية الوصول** ♿
- تباين ألوان عالي (WCAG 2.1 AA)
- دعم قراء الشاشة
- مناسب لذوي القدرات المحدودة

---

## 🚀 كيفية المساهمة

### 1. إعداد البيئة المحلية

#### المتطلبات
```bash
# تأكد من تثبيت Flutter
flutter --version  # يجب أن يكون >= 3.0.0

# تأكد من تثبيت Dart
dart --version     # يجب أن يكون >= 3.0.0
```

#### الإعداد
```bash
# 1. Fork المشروع على GitHub

# 2. استنساخ Fork الخاص بك
git clone https://github.com/YOUR_USERNAME/cancare.git
cd cancare/flutter_application_5

# 3. إضافة المشروع الأصلي كـupstream
git remote add upstream https://github.com/ORIGINAL_OWNER/cancare.git

# 4. تثبيت التبعيات
flutter pub get

# 5. التأكد من عمل التطبيق
flutter run
```

### 2. إنشاء فرع جديد

```bash
# أنشئ فرع للميزة أو الإصلاح
git checkout -b feature/amazing-feature
# أو
git checkout -b fix/bug-description
```

**تسمية الفروع**:
- `feature/` - للميزات الجديدة
- `fix/` - لإصلاح الأخطاء
- `docs/` - للتوثيق
- `refactor/` - لإعادة الهيكلة
- `style/` - للتحسينات التصميمية

### 3. إجراء التغييرات

#### قبل البدء:
- ✅ اقرأ الكود الموجود وافهم البنية
- ✅ راجع [REDESIGN_GUIDE.md](REDESIGN_GUIDE.md) لفهم المبادئ التصميمية
- ✅ تأكد من أن تغييراتك متوافقة مع نظام الألوان والخطوط الحالي

#### أثناء التطوير:
```bash
# تشغيل التطبيق مع hot reload
flutter run

# تحليل الكود
flutter analyze

# اختبار التطبيق
flutter test
```

### 4. كتابة الكود

#### قواعد كتابة الكود:

##### أ. التعليقات
```dart
// ✅ جيد: تعليقات واضحة بالعربية
/// بناء قسم المواعيد القادمة
/// 
/// يعرض قائمة بالمواعيد الطبية القادمة للمريض
/// مع إمكانية الضغط للتفاصيل
Widget _buildUpcomingAppointments(ThemeData theme) {
  // TODO: Backend Integration - Fetch appointments from API
  return Container(
    // تصميم الكرت...
  );
}

// ❌ سيء: بدون تعليقات
Widget _buildUpcomingAppointments(ThemeData theme) {
  return Container();
}
```

##### ب. التسمية
```dart
// ✅ جيد: أسماء واضحة ومفهومة
final String _userName;
final List<Map<String, dynamic>> _upcomingAppointments;

// ❌ سيء: أسماء مبهمة
final String _n;
final List<Map<String, dynamic>> _data;
```

##### ج. الألوان
```dart
// ✅ جيد: استخدام الألوان من Theme
color: theme.colorScheme.primary,
backgroundColor: theme.scaffoldBackgroundColor,

// أو الألوان المُعرّفة في AppTheme
color: AppTheme.patientPrimary,

// ❌ سيء: ألوان hardcoded عشوائية
color: Color(0xFF123456),
```

##### د. الخطوط
```dart
// ✅ جيد: استخدام أحجام من Theme
style: theme.textTheme.headlineLarge,
style: theme.textTheme.bodyMedium,

// ❌ سيء: أحجام hardcoded
style: TextStyle(fontSize: 15),
```

##### هـ. المسافات
```dart
// ✅ جيد: استخدام ثوابت المسافات
const EdgeInsets.all(AppTheme.patientSpacingMD),
const SizedBox(height: AppTheme.patientSpacingLG),

// مقبول: مسافات محددة للحالات الخاصة
const EdgeInsets.symmetric(horizontal: 16, vertical: 12),

// ❌ سيء: مسافات عشوائية
const EdgeInsets.all(7.5),
```

### 5. الاختبار

#### اختبار يدوي:
```bash
# على Android
flutter run -d android

# على iOS
flutter run -d ios

# على محاكي
flutter emulators --launch emulator_id
flutter run
```

#### قائمة التحقق قبل الإرسال:
- [ ] التطبيق يعمل بدون أخطاء
- [ ] الألوان متوافقة مع نظام الألوان الجديد
- [ ] الخطوط بأحجام مناسبة
- [ ] الأزرار كبيرة وسهلة الضغط (64 بكسل للمرضى)
- [ ] المسافات مناسبة وسخية
- [ ] التنقل واضح وبديهي
- [ ] الرسائل مطمئنة وإيجابية
- [ ] لا توجد أخطاء lint (`flutter analyze`)

### 6. Commit التغييرات

#### تنسيق رسائل Commit:
```
[نوع]: وصف مختصر بالعربية

وصف تفصيلي إن لزم الأمر

إغلاق: #رقم_المشكلة
```

**أنواع Commit**:
- `feat`: ميزة جديدة
- `fix`: إصلاح خطأ
- `docs`: تحديث التوثيق
- `style`: تحسينات تصميمية
- `refactor`: إعادة هيكلة الكود
- `test`: إضافة/تحديث اختبارات
- `chore`: مهام صيانة

**أمثلة**:
```bash
git commit -m "feat: إضافة زر SOS في واجهة المريض"
git commit -m "fix: إصلاح تنسيق قسم الأدوية"
git commit -m "style: تحديث ألوان أزرار الطبيب"
git commit -m "docs: تحديث دليل المساهمة"
```

### 7. Push التغييرات

```bash
# Push للفرع الخاص بك
git push origin feature/amazing-feature
```

### 8. فتح Pull Request

1. اذهب إلى صفحة GitHub الخاصة بـFork الخاص بك
2. اضغط على "New Pull Request"
3. اختر الفرع الخاص بك
4. املأ النموذج:

```markdown
## الوصف
وصف مختصر للتغييرات

## نوع التغيير
- [ ] ميزة جديدة
- [ ] إصلاح خطأ
- [ ] تحسين تصميمي
- [ ] تحديث توثيق

## لقطات الشاشة
(إن وجدت)

## قائمة التحقق
- [ ] الكود يتبع معايير المشروع
- [ ] التعليقات واضحة بالعربية
- [ ] الألوان متوافقة مع نظام الألوان
- [ ] الخطوط بأحجام مناسبة
- [ ] تم اختبار التغييرات
- [ ] لا توجد أخطاء lint
```

---

## 📏 معايير الكود

### 1. البنية

```dart
/// مثال على بنية صفحة

import 'package:flutter/material.dart';
// الاستيرادات الأخرى...

/// وصف الصفحة بالعربية
///
/// تفاصيل إضافية عن الصفحة ووظائفها
class MyPage extends StatefulWidget {
  const MyPage({super.key});

  @override
  State<MyPage> createState() => _MyPageState();
}

class _MyPageState extends State<MyPage> {
  // ==================== State Variables ====================
  // متغيرات الحالة هنا...

  // ==================== Lifecycle Methods ====================
  @override
  void initState() {
    super.initState();
    // TODO: Backend Integration - Load data
  }

  @override
  void dispose() {
    // تنظيف الموارد
    super.dispose();
  }

  // ==================== UI Build Methods ====================
  @override
  Widget build(BuildContext context) {
    final theme = Theme.of(context);
    return Scaffold(
      // البناء هنا...
    );
  }

  // ==================== Helper Methods ====================
  Widget _buildSection(ThemeData theme) {
    // بناء قسم معين...
  }
}
```

### 2. التنسيق

```bash
# تنسيق تلقائي للكود
flutter format .

# أو في VS Code: Shift + Alt + F
# أو في Android Studio: Ctrl + Alt + L
```

### 3. التحليل

```bash
# تحليل الكود للأخطاء والتحذيرات
flutter analyze

# يجب أن يكون الناتج: "No issues found!"
```

---

## 🔍 عملية المراجعة

### ما نبحث عنه:

#### 1. **التصميم** 🎨
- [ ] الألوان متوافقة مع `AppTheme`
- [ ] الخطوط بأحجام مناسبة
- [ ] الأزرار كبيرة وواضحة
- [ ] المسافات سخية ومريحة
- [ ] التباين عالي ومقروء

#### 2. **الوظيفة** ⚙️
- [ ] الكود يعمل بشكل صحيح
- [ ] لا توجد أخطاء أو تحذيرات
- [ ] التنقل سلس ومنطقي
- [ ] الرسائل واضحة ومفهومة

#### 3. **الكود** 💻
- [ ] التعليقات واضحة بالعربية
- [ ] الأسماء مفهومة ومعبرة
- [ ] لا يوجد كود مكرر
- [ ] يتبع معايير Flutter
- [ ] TODO markers واضحة للتكامل المستقبلي

#### 4. **الوثائق** 📚
- [ ] التعليقات شاملة
- [ ] تحديث الملفات ذات الصلة
- [ ] أمثلة واضحة إن لزم

### مدة المراجعة:
- عادة 2-7 أيام
- قد نطلب تغييرات
- سنتواصل عبر التعليقات في PR

---

## 🐛 الإبلاغ عن الأخطاء

### قبل الإبلاغ:
1. ✅ تأكد أن الخطأ موجود في آخر إصدار
2. ✅ ابحث في Issues الموجودة
3. ✅ حاول إعادة إنتاج الخطأ

### تقرير الخطأ:
استخدم هذا النموذج:

```markdown
## وصف الخطأ
وصف واضح ومختصر للخطأ

## خطوات إعادة الإنتاج
1. اذهب إلى '...'
2. اضغط على '...'
3. انظر الخطأ

## السلوك المتوقع
ما كان يجب أن يحدث

## السلوك الفعلي
ما حدث فعلاً

## لقطات الشاشة
(إن وجدت)

## البيئة
- نظام التشغيل: [e.g. Android 12, iOS 16]
- إصدار Flutter: [e.g. 3.16.0]
- إصدار التطبيق: [e.g. 2.0.0]

## معلومات إضافية
أي معلومات أخرى مفيدة
```

---

## 💡 اقتراح ميزات جديدة

### قبل الاقتراح:
1. ✅ تأكد أن الميزة غير موجودة
2. ✅ ابحث في Issues لاقتراحات مشابهة
3. ✅ فكر في الفائدة للمرضى

### نموذج الاقتراح:

```markdown
## المشكلة
وصف المشكلة التي تحلها هذه الميزة

## الحل المقترح
وصف تفصيلي للميزة المقترحة

## البدائل المدروسة
هل فكرت في حلول بديلة؟

## الفائدة للمستخدمين
كيف ستساعد هذه الميزة المرضى/الأطباء/الممرضين؟

## تصور التصميم
(mockups أو رسومات توضيحية إن أمكن)

## معلومات إضافية
أي تفاصيل أخرى مهمة
```

---

## 🎨 دليل التصميم

### الألوان

#### للمرضى:
```dart
// الأساسي
AppTheme.patientPrimary      // #81C784 (أخضر ناعم)
AppTheme.patientSecondary    // #5B9AA0 (فيروزي)

// الخلفية
AppTheme.patientBackground   // #F7FBF9 (نعناعي خفيف)
AppTheme.patientSurface      // #FFFFFF (أبيض)
```

#### للأطباء:
```dart
AppTheme.doctorPrimary       // #5B9AA0 (فيروزي)
AppTheme.doctorSecondary     // #42A5F5 (أزرق)
```

#### للممرضين:
```dart
AppTheme.nursePrimary        // #9575CD (بنفسجي)
AppTheme.nurseSecondary      // #81C784 (أخضر)
```

### الخطوط

```dart
// للمرضى (أكبر)
theme.textTheme.headlineLarge   // 26px
theme.textTheme.titleLarge      // 20px
theme.textTheme.bodyLarge       // 19px

// للأطباء والممرضين (معتدل)
theme.textTheme.headlineLarge   // 20-18px
theme.textTheme.bodyLarge       // 16-15px
```

### المسافات

```dart
// للمرضى (أوسع)
AppTheme.patientSpacingSM    // 16px
AppTheme.patientSpacingMD    // 20px
AppTheme.patientSpacingLG    // 28px

// للأطباء والممرضين (معتدل)
AppTheme.staffSpacingSM      // 8px
AppTheme.staffSpacingMD      // 12px
```

### الأزرار

```dart
// للمرضى (أكبر)
height: AppTheme.patientButtonHeight,  // 64px

// للأطباء والممرضين (معتدل)
height: AppTheme.staffButtonHeight,    // 48px
```

---

## 📞 التواصل

### القنوات:
- 💬 **GitHub Issues**: للأخطاء والاقتراحات
- 📧 **البريد الإلكتروني**: dev@cancare.com
- 💭 **Discussions**: للأسئلة العامة

### أوقات الرد:
- Issues: عادة 1-3 أيام
- Pull Requests: 2-7 أيام
- البريد الإلكتروني: 3-5 أيام عمل

---

## 🙏 شكراً

شكراً لك على اهتمامك بالمساهمة في CanCare!

مساهمتك تساعد في تحسين تجربة مرضى السرطان وعائلاتهم. 💚

---

**آخر تحديث**: 4 يناير 2026

